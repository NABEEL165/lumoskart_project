<!DOCTYPE html>
{% load static %}
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Influencer Application - Lumoskart</title>
    <!-- Favicon -->
    <link rel="shortcut icon" href="{% static 'accounts/images/favicon.png' %}" type="image/x-icon">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --accent-color: #4cc9f0;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --success-color: #4ade80;
            --warning-color: #facc15;
            --danger-color: #f87171;
            --gray-100: #f8f9fa;
            --gray-200: #e9ecef;
            --gray-300: #dee2e6;
            --gray-400: #ced4da;
            --gray-500: #adb5bd;
            --gray-600: #6c757d;
            --gray-700: #495057;
            --gray-800: #343a40;
            --gray-900: #212529;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #e4edf5 100%);
            min-height: 100vh;
            padding-bottom: 2rem;
        }

        .application-container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .application-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            position: relative;
        }

        .application-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 8px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
        }

        .application-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 2rem;
            text-align: center;
            position: relative;
        }

        .application-header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

        .application-header p {
            font-size: 1.1rem;
            opacity: 0.9;
            margin-bottom: 0;
        }

        .application-body {
            padding: 2.5rem;
        }

        .form-section {
            margin-bottom: 2.5rem;
            padding: 1.5rem;
            border-radius: 15px;
            background: var(--gray-100);
            border-left: 4px solid var(--primary-color);
            transition: all 0.3s ease;
        }

        .form-section:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
        }

        .form-section h3 {
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 600;
            font-size: 1.4rem;
        }

        .form-label {
            font-weight: 600;
            color: var(--gray-800);
            margin-bottom: 0.5rem;
            display: block;
        }

        .form-control, .form-select {
            padding: 0.875rem 1rem;
            border: 2px solid var(--gray-300);
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background-color: white;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.25);
            outline: none;
        }

        .form-text {
            color: var(--gray-600);
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }

        .social-handle-input .input-group {
            border-radius: 10px;
            overflow: hidden;
            border: 2px solid var(--gray-300);
        }

        .social-handle-input .input-group-text {
            background-color: var(--gray-100);
            border: none;
            font-weight: 600;
            color: var(--gray-700);
        }

        .social-handle-input input {
            border: none;
        }

        .social-handle-input input:focus {
            box-shadow: none;
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border: none;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.3s ease;
            width: 100%;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.3);
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(67, 97, 238, 0.4);
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
        }

        .text-danger {
            color: var(--danger-color) !important;
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }

        .alert {
            border-radius: 12px;
            padding: 1rem 1.5rem;
        }

        /* Responsive adjustments */
        @media (max-width: 992px) {
            .application-container {
                margin: 1.5rem auto;
                padding: 0 0.75rem;
            }

            .application-header {
                padding: 1.75rem;
            }

            .application-header h1 {
                font-size: 2.2rem;
            }

            .application-body {
                padding: 2rem;
            }
        }

        @media (max-width: 768px) {
            .application-container {
                margin: 1rem auto;
                padding: 0 0.5rem;
            }

            .application-header {
                padding: 1.5rem;
            }

            .application-header h1 {
                font-size: 2rem;
            }

            .application-header p {
                font-size: 1.05rem;
            }

            .application-body {
                padding: 1.5rem;
            }

            .form-section {
                padding: 1.25rem;
                margin-bottom: 1.5rem;
            }

            .form-section h3 {
                font-size: 1.3rem;
            }

            .btn-submit {
                padding: 0.875rem 1.5rem;
                font-size: 1rem;
            }

            /* Stack form elements on mobile */
            .row {
                flex-direction: column;
            }

            .row > [class*="col-"] {
                width: 100%;
                margin-bottom: 1rem;
            }

            .row > [class*="col-"]:last-child {
                margin-bottom: 0;
            }
        }

        @media (max-width: 576px) {
            .application-header {
                padding: 1.25rem;
            }

            .application-header h1 {
                font-size: 1.75rem;
            }

            .application-header p {
                font-size: 1rem;
            }

            .application-body {
                padding: 1.25rem 1rem;
            }

            .form-section {
                padding: 1rem;
            }

            .form-section h3 {
                font-size: 1.2rem;
            }

            .btn-submit {
                padding: 0.875rem 1.25rem;
                font-size: 0.95rem;
            }

            .form-label {
                font-size: 0.95rem;
            }

            .form-control, .form-select {
                padding: 0.75rem 0.9rem;
                font-size: 0.95rem;
            }
        }

        @media (max-width: 425px) {
            .application-container {
                margin: 0.75rem auto;
                padding: 0 0.25rem;
            }

            .application-header {
                padding: 1rem;
            }

            .application-header h1 {
                font-size: 1.5rem;
            }

            .application-header p {
                font-size: 0.9rem;
            }

            .application-body {
                padding: 1rem;
            }

            .form-section {
                padding: 0.875rem;
                margin-bottom: 1.25rem;
            }

            .form-section h3 {
                font-size: 1.1rem;
            }

            .btn-submit {
                padding: 0.75rem 1rem;
                font-size: 0.9rem;
            }

            .form-label {
                font-size: 0.9rem;
            }

            .form-control, .form-select {
                padding: 0.65rem 0.8rem;
                font-size: 0.9rem;
            }
        }

        .company-name {
            font-weight: 700;
            color: var(--primary-color);
        }
         .navbar-brand:hover .brand-text {
        color: #ffffff;
        text-shadow: 0 0 10px rgba(0, 199, 247, 0.5);
        /* Teal glow using your primary color */
        transition: all 0.3s ease;
    }

    .navbar-brand:hover .logo-img {
        transform: scale(1.05);
        transition: all 0.3s ease;
    }

    /* Mobile Navbar Optimization */
    @media (max-width: 991px) {
        .navbar-container {
            display: flex !important;
            flex-wrap: nowrap !important;
            justify-content: space-between !important;
            align-items: center !important;
            width: 100% !important;
            padding: 0 5px !important;
        }

        .navbar-brand {
            margin-right: 0 !important;
            gap: 10px !important;
            flex-shrink: 0;
            display: flex !important;
            align-items: center !important;
        }

        .logo-img {
            max-height: 40px !important;
            width: auto !important;
            flex-shrink: 0;
        }

        .brand-text {
            font-size: 1.4rem !important;
            display: inline-block !important;
            color: #ffffff !important;
            background: linear-gradient(135deg, #ffffff 0%, #00c7f7 100%) !important;
            -webkit-background-clip: text !important;
            background-clip: text !important;
            -webkit-text-fill-color: transparent !important;
            white-space: nowrap !important;
        }

        .mobile-view {
            display: flex !important;
            align-items: center !important;
            gap: 5px !important;
            flex-shrink: 0;
        }

        .desktop-view {
            display: none !important;
        }

        .btn-open-app {
            padding: 4px 12px !important;
            font-size: 0.85rem !important;
            height: fit-content;
        }
    }


    /* Custom Toggler Icon and Buttons */
    .navbar-toggler {
        border: 1px solid rgba(255, 255, 255, 0.2) !important;
        padding: 5px 8px !important;
    }

    .navbar-toggler-icon {
        background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba%28255, 255, 255, 0.8%29' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e") !important;
        width: 1.25em !important;
        height: 1.25em !important;
    }

    .mobile-view .btn-outline-light {
        border: 1px solid rgba(255, 255, 255, 0.2) !important;
        background: rgba(255, 255, 255, 0.05);
        color: #ffffff !important;
    }

    .mobile-view .btn-outline-light:hover {
        background: rgba(255, 255, 255, 0.1);
    }
    </style>
</head>
<body>
    <!--<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">-->
    <!--    <div class="container-fluid px-4 py-2">-->
    <!--        <a class="navbar-brand d-flex align-items-center text-decoration-none" href="{% url 'home' %}">-->
    <!--            <img src="{% static 'accounts/images/Lumoskart.png' %}" alt="Lumoskart Logo" class="navbar-logo" style="height: 40px;">-->
    <!--            <span class="company-name ms-2 fs-4 fw-bold">Lumoskart</span>-->
    <!--        </a>-->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid navbar-container">
            <a href="{% url 'home' %}" class="navbar-brand d-flex align-items-center gap-3">
                <img src="{% static 'accounts/images/WhatsApp Image 2026-01-07 at 6.45.12 PM.jpeg' %}"
                    alt="LumosKart Logo" class="logo-img" style="max-height: 50px; width: auto;">
                <span class="brand-text">LumosKart</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a href="{% url 'home' %}" class="nav-link fw-semibold text-dark"><i class="fas fa-home me-1"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a href="{% url 'about_us' %}" class="nav-link fw-semibold text-dark"><i class="fas fa-info-circle me-1"></i> About</a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link fw-semibold text-primary active"><i class="fas fa-user-plus me-1"></i> Apply as Influencer</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="application-container">
        <div class="application-card">
            <div class="application-header">
                <h1>Become an Influencer</h1>
                <p>Join our network of trusted content creators and grow your influence</p>
            </div>

            <div class="application-body">
                {% if messages %}
                    {% for message in messages %}
                        <div class="alert alert-{{ message.tags }} alert-dismissible fade show rounded-3 mb-4" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% endfor %}
                {% endif %}

                <form method="post" enctype="multipart/form-data">
                    {% csrf_token %}

                    <div class="form-section">
                        <h3><i class="fas fa-user-circle text-primary"></i> Personal Information</h3>
                        <div class="row g-4">
                            <div class="col-md-12 mb-3">
                                <label for="{{ form.bio.id_for_label }}" class="form-label">About Yourself</label>
                                {{ form.bio }}
                                {% if form.bio.errors %}
                                    <div class="text-danger">{{ form.bio.errors }}</div>
                                {% endif %}
                                <div class="form-text">Tell us about yourself, your interests, and what makes you unique as an influencer</div>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label for="{{ form.followers_count.id_for_label }}" class="form-label">Follower Count</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-users"></i></span>
                                    {{ form.followers_count }}
                                </div>
                                {% if form.followers_count.errors %}
                                    <div class="text-danger">{{ form.followers_count.errors }}</div>
                                {% endif %}
                                <div class="form-text">Approximate number of followers across all platforms</div>
                            </div>
                        </div>
                    </div>

                    <div class="form-section">
                        <h3><i class="fas fa-hashtag text-primary"></i> Social Media Handles</h3>
                        <div class="row g-4">
                            <div class="col-md-6 mb-3">
                                <label for="{{ form.instagram_handle.id_for_label }}" class="form-label">Instagram Handle</label>
                                <div class="input-group social-handle-input">
                                    <span class="input-group-text">@</span>
                                    {{ form.instagram_handle }}
                                </div>
                                {% if form.instagram_handle.errors %}
                                    <div class="text-danger">{{ form.instagram_handle.errors }}</div>
                                {% endif %}
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="{{ form.youtube_channel.id_for_label }}" class="form-label">YouTube Channel</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fab fa-youtube"></i></span>
                                    {{ form.youtube_channel }}
                                </div>
                                {% if form.youtube_channel.errors %}
                                    <div class="text-danger">{{ form.youtube_channel.errors }}</div>
                                {% endif %}
                            </div>
                        </div>
                        <div class="row g-4">
                            <div class="col-md-6 mb-3">
                                <label for="{{ form.tiktok_handle.id_for_label }}" class="form-label">TikTok Handle</label>
                                <div class="input-group social-handle-input">
                                    <span class="input-group-text">@</span>
                                    {{ form.tiktok_handle }}
                                </div>
                                {% if form.tiktok_handle.errors %}
                                    <div class="text-danger">{{ form.tiktok_handle.errors }}</div>
                                {% endif %}
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="{{ form.other_social.id_for_label }}" class="form-label">Other Social Media</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-link"></i></span>
                                    {{ form.other_social }}
                                </div>
                                {% if form.other_social.errors %}
                                    <div class="text-danger">{{ form.other_social.errors }}</div>
                                {% endif %}
                            </div>
                        </div>
                    </div>

                    <div class="form-section">
                        <h3><i class="fas fa-tag text-primary"></i> Category & Niche</h3>
                        <div class="row g-4">
                            <div class="col-md-12 mb-3">
                                <label for="{{ form.category.id_for_label }}" class="form-label">Category/Niche</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-list"></i></span>
                                    {{ form.category }}
                                </div>
                                {% if form.category.errors %}
                                    <div class="text-danger">{{ form.category.errors }}</div>
                                {% endif %}
                            </div>
                        </div>
                    </div>

                    <div class="form-section">
                        <h3><i class="fas fa-link text-primary"></i> Portfolio & Content</h3>
                        <div class="mb-3">
                            <label for="{{ form.portfolio_links.id_for_label }}" class="form-label">Portfolio Links</label>
                            {{ form.portfolio_links }}
                            {% if form.portfolio_links.errors %}
                                <div class="text-danger">{{ form.portfolio_links.errors }}</div>
                            {% endif %}
                            <div class="form-text">Add links to your portfolio, previous work, etc. (one per line)</div>
                        </div>

                        <div class="mb-3">
                            <label for="{{ form.video_upload.id_for_label }}" class="form-label">Sample Video (Optional)</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-video"></i></span>
                                {{ form.video_upload }}
                            </div>
                            {% if form.video_upload.errors %}
                                <div class="text-danger">{{ form.video_upload.errors }}</div>
                            {% endif %}
                            <div class="form-text">Upload a sample of your content (MP4, MOV, or AVI, max 100MB)</div>
                        </div>
                    </div>

                    <div class="d-grid gap-3 mt-5">
                        <button type="submit" class="btn btn-submit btn-lg">
                            <i class="fas fa-paper-plane me-2"></i>Submit Application
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap 5 JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Font Awesome -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>

    <script>
        // Add some interactive effects
        document.addEventListener('DOMContentLoaded', function() {
            // Add focus effects to form sections
            const formSections = document.querySelectorAll('.form-section');
            formSections.forEach(section => {
                section.addEventListener('click', function() {
                    this.style.transform = 'translateY(-2px)';
                    setTimeout(() => {
                        this.style.transform = '';
                    }, 300);
                });
            });

            // Add input focus effects
            const inputs = document.querySelectorAll('input, textarea, select');
            inputs.forEach(input => {
                input.addEventListener('focus', function() {
                    this.style.transform = 'scale(1.02)';
                });

                input.addEventListener('blur', function() {
                    this.style.transform = '';
                });
            });
        });
    </script>
</body>
</html>
